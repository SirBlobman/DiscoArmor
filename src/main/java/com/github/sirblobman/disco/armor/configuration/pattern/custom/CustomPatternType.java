package com.github.sirblobman.disco.armor.configuration.pattern.custom;

public enum CustomPatternType {
    /**
     * Configured animation frames and durations.
     */
    FRAMES,

    /**
     * Copy settings from a different armor slot.
     */
    COPY,

    /**
     * Doesn't change, always the same values.
     */
    FIXED
}
